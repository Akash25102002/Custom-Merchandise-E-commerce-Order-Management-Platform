{
  "name": "custom-merchandise-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "npm run build && cd backend && npm start",
    "serve": "cd backend && npm start"
  }
}
